import { ComponentChildren } from 'preact/src';
import { TestIdProps } from '../hooks/UNSAFE_useTestId';
export type BaseSelectorProps = TestIdProps & {
    /**
     * aria-label for this selectorAll
     */
    'aria-label'?: string;
    /**
     * One or more ids (separated by spaces) of elements that label the selected item. This will be used for the aria-labelledby of the DOM element.
     */
    'aria-labelledby'?: string;
    /**
     * Whether the element should be checked, unchecked, or in a partial state.
     */
    isChecked: 'checked' | 'unchecked' | 'partial';
    /**
     * merged props that pass in the base selector component
     */
    tooltipProps?: Record<string, any>;
    /**
     * Callback function for selection situation in Selector
     */
    onSelectionHandler: (e: MouseEvent | KeyboardEvent) => void;
    /**
     * The tooltipContent
     */
    tooltipContent?: ComponentChildren;
};
