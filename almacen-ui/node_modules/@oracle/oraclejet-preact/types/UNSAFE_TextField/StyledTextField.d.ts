import type { ContextType, Ref } from 'preact';
import type { StyledComponentProps } from "../PRIVATE_oraclejet-internal-utilities/typeUtils";
import type { FormVariantContext } from "../hooks/UNSAFE_useFormVariantContext";
import { type LayoutColumnSpan } from "../utils/UNSAFE_styles/Layout";
export type StyleProps = {
    columnSpan?: LayoutColumnSpan;
    variant?: ContextType<typeof FormVariantContext>;
};
type Props = StyledComponentProps<'div', StyleProps>;
/**
 * StyledTextField is the main root container for all text field layouts.
 * It renders a <div> with OracleJET theme-based classes, applying styles
 * and layout variants for text fields. This component does not handle any
 * business logic or validation, but ensures correct styling is applied
 * based on props such as columnSpan, variant, input group context, readonly/embedded modes, etc.
 * All event handlers, refs, and children are passed through to the div.
 */
export declare const StyledTextField: import("preact").FunctionalComponent<import("preact/compat").PropsWithoutRef<Props> & {
    ref?: Ref<HTMLElement> | undefined;
}>;
export {};
