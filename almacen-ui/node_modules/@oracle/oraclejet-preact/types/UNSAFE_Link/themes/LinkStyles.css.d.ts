import { CSSProperties } from '@vanilla-extract/css';
import { linkVars } from './LinkContract.css';
import { ComponentThemeType, VariantOptions, CompoundVariantStyles } from '../../UNSAFE_Theme';
type LinkVariants = typeof variants;
type LinkVariantOptions = VariantOptions<LinkVariants>;
type LinkStyles = typeof styles;
type LinkTheme = ComponentThemeType<LinkVariants, LinkStyles>;
declare const styles: {
    standard: string;
};
/*******************
 * Style Variants
 *******************/
declare const variants: {
    safariStyles: {
        isSafari: {
            selectors: {
                '&::after': {
                    content: string;
                    display: string;
                };
            };
        };
        notSafari: {};
    };
    variant: {
        standard: {};
        primary: {
            vars: {
                [linkVars.textColor]: `var(--${string})` | `var(--${string}, ${string})` | `var(--${string}, ${number})`;
                [linkVars.textColorVisited]: `var(--${string})` | `var(--${string}, ${string})` | `var(--${string}, ${number})`;
                [linkVars.textColorActive]: `var(--${string})` | `var(--${string}, ${string})` | `var(--${string}, ${number})`;
            };
        };
        secondary: {
            vars: {
                [linkVars.textColor]: `var(--${string})` | `var(--${string}, ${string})` | `var(--${string}, ${number})`;
                [linkVars.textColorVisited]: `var(--${string})` | `var(--${string}, ${string})` | `var(--${string}, ${number})`;
                [linkVars.textColorActive]: `var(--${string})` | `var(--${string}, ${string})` | `var(--${string}, ${number})`;
            };
        };
    };
    placement: {
        standalone: {
            alignItems: string;
            textDecorationLine: `var(--${string})` | `var(--${string}, ${string})` | `var(--${string}, ${number})`;
            minHeight: string;
            minWidth: string;
            display: string;
            fontWeight: `var(--${string})` | `var(--${string}, ${string})` | `var(--${string}, ${number})`;
            selectors: {
                '&:active': {
                    textDecorationLine: `var(--${string})` | `var(--${string}, ${string})` | `var(--${string}, ${number})`;
                };
            };
        };
        embedded: {
            textDecorationLine: `var(--${string})` | `var(--${string}, ${string})` | `var(--${string}, ${number})`;
            selectors: {
                '&:active': {
                    textDecorationLine: `var(--${string})` | `var(--${string}, ${string})` | `var(--${string}, ${number})`;
                };
            };
        };
    };
    needsEventsHover: {
        isNeedsEventsHover: string;
        notNeedsEventsHover: string;
    };
    pseudoHover: {
        isPseudoHover: string;
        notPseudoHover: string;
    };
    disabled: {
        notDisabled: {};
        isDisabled: {
            vars: {
                [linkVars.textColor]: `var(--${string})` | `var(--${string}, ${string})` | `var(--${string}, ${number})`;
            };
            selectors: {
                '&:active': {
                    textDecorationLine: string;
                };
            };
        };
    };
    truncatingLinkStyles: {
        styles: {
            overflow: string;
            textOverflow: string;
            verticalAlign: string;
        };
        none: {};
    };
    truncatingLinkWhiteSpace: {
        nowrap: {
            whiteSpace: CSSProperties["whiteSpace"];
        };
        none: {};
    };
};
declare const compoundVariants: CompoundVariantStyles<LinkVariantOptions>;
export type { LinkVariantOptions, LinkStyles, LinkTheme };
export { variants, styles, compoundVariants };
