/* @oracle/oraclejet-preact: undefined */
import { k as keyExtractor } from './collectionUtils-5313629f.js';
import { g as getClientHints } from './clientHints-a42b510c.js';

/**
 * Helper function to get the row action context from a pointer or keyboard event
 */
const getRowActionGestureContext = (rootId, collectionData, targetElement, rowSelector, getActionContext, targetCell) => {
    let rowKey;
    if (targetCell?.type === 'data') {
        rowKey = targetCell.rowKey;
    }
    else {
        rowKey = keyExtractor(targetElement, `[${rowSelector}='${rootId}']`);
    }
    if (rowKey != null) {
        for (let i = 0; i < collectionData.length; i++) {
            const dataItem = collectionData[i];
            if (rowKey === dataItem.metadata.key) {
                return getActionContext(i, rowKey, dataItem.data);
            }
        }
    }
    return undefined;
};
/**
 * Helper function to handle a row action gesture from a pointer or keyboard event
 */
const handleRowActionGesture = (rootId, collectionData, onRowAction, targetElement, rowSelector, getActionContext, targetCell) => {
    const rowActionContext = getRowActionGestureContext(rootId, collectionData, targetElement, rowSelector, getActionContext, targetCell);
    if (rowActionContext != null) {
        onRowAction({ context: rowActionContext, target: targetElement });
        return true;
    }
    return false;
};

/**
 * @license
 * Copyright (c) %FIRST_YEAR% %CURRENT_YEAR%, Oracle and/or its affiliates.
 * The Universal Permissive License (UPL), Version 1.0
 * as shown at https://oss.oracle.com/licenses/upl/
 * @ignore
 */
const isLogicalCtrl = (event) => {
    return getClientHints().platform === 'mac' ? event.metaKey : event.ctrlKey;
};

export { getRowActionGestureContext as g, handleRowActionGesture as h, isLogicalCtrl as i };
//# sourceMappingURL=EventUtils-e1b93d3a.js.map
