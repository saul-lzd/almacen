/* @oracle/oraclejet-preact: undefined */
import { jsx } from 'preact/jsx-runtime';
import { Fragment } from 'preact';

/**
 * Component that renders an array of data.
 */
function Collection({ items, indexOffset = 0, children }) {
    return (jsx(Fragment, { children: items.map((data, index) => {
            const ctx = { index: index + indexOffset, data };
            return children(ctx);
        }) }));
}

export { Collection as C };
//# sourceMappingURL=Collection-e1ef8b8d.js.map
