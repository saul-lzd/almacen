/* @oracle/oraclejet-preact: undefined */
import { jsx } from 'preact/jsx-runtime';
import { u as useTestId } from './useTestId-f24ac480.js';
import { colorPickerPropVars } from './UNSAFE_ColorPicker/themes/ColorPickerContract.css.js';
import { u as useComponentTheme } from './useComponentTheme-d2f9e47f.js';
import { ColorPickerRedwoodTheme } from './UNSAFE_ColorPicker/themes/redwood/ColorPickerTheme.js';
import { forwardRef } from 'preact/compat';
import { F as Flex } from './Flex-03129963.js';
import { i as isMobile } from './clientUtils-71f2dbaf.js';
import { C as ColorPalette } from './ColorPalette-950c6fc6.js';

/**
 * A ColorPicker is a component that allows users to select a single color.
 */
const ColorPicker = forwardRef(({ swatchSize = 'xs', palette, height, maxWidth, value, testId, width, onCommit }, pickerRef) => {
    // we need to render the baseTheme on the root dom element so that the CSS vars in colorPickerPropVars will be defined.
    const { classes } = useComponentTheme(ColorPickerRedwoodTheme);
    const testIdProps = useTestId(testId);
    return (jsx("div", { ref: pickerRef, class: classes, ...testIdProps, children: jsx(Flex, { direction: "column", height: height === 'auto' ? undefined : isMobile() ? undefined : colorPickerPropVars.maxHeight, maxWidth: maxWidth !== undefined ? maxWidth : isMobile() ? '100%' : colorPickerPropVars.maxWidth, width: width !== undefined ? width : colorPickerPropVars.width, children: jsx(ColorPalette, { palette: palette, swatchSize: swatchSize, value: value, testId: testId ? testId + '_picker' : undefined, onCommit: onCommit }) }) }));
});

export { ColorPicker as C };
//# sourceMappingURL=ColorPicker-ae648c4c.js.map
