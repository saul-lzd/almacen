/* @oracle/oraclejet-preact: undefined */
import { jsx, jsxs } from 'preact/jsx-runtime';
import { Fragment } from 'preact';
import { C as Collection } from './Collection-e1ef8b8d.js';
import { u as useViewportIntersect } from './useViewportIntersect-6ff65310.js';

const LOADMORE_STYLE_CLASS = 'oj-collection-loadmore';
const DEFAULT_MARGIN = 1;
const LOAD_MORE_SELECTOR = `.${LOADMORE_STYLE_CLASS}`;
/**
 * Component that only render items in the specified viewport.
 * @param props
 */
function LoadMoreCollection({ data, children, viewportConfig, hasMore, onLoadMore, loadMoreIndicator, suggestionCount, suggestions, suggestionsContainerClasses, loadMoreThreshold = DEFAULT_MARGIN }) {
    // detect if viewport has changed and it needs to re-render based on a new range
    useViewportIntersect(viewportConfig, Math.max(loadMoreThreshold, DEFAULT_MARGIN), 0, LOAD_MORE_SELECTOR, () => {
        if (data) {
            onLoadMore();
        }
    });
    // if DataState is invalid (including not specified), just render blank
    if (!data) {
        return jsx("div", {});
    }
    // Provides a more robust suggestions rendering for non-virtualized cases
    if (suggestionCount != null && suggestions != null) {
        let suggestionData;
        let collectionData;
        if (data.length <= suggestionCount) {
            suggestionData = data;
            collectionData = [];
        }
        else {
            suggestionData = data.slice(0, suggestionCount);
            collectionData = data.slice(suggestionCount);
        }
        return (jsxs(Fragment, { children: [jsxs("div", { class: suggestionsContainerClasses, children: [suggestions, jsx(Collection, { items: suggestionData, children: children })] }), jsx(Collection, { items: collectionData, indexOffset: suggestionCount, children: children }), hasMore && loadMoreIndicator] }));
    }
    // might need to support elementType prop in the future to allow customization of what
    // type of element to render for the root
    return (jsxs(Fragment, { children: [suggestions, jsx(Collection, { items: data, children: children }), hasMore && loadMoreIndicator] }));
}

export { LOADMORE_STYLE_CLASS as L, LoadMoreCollection as a };
//# sourceMappingURL=LoadMoreCollection-d9f22790.js.map
