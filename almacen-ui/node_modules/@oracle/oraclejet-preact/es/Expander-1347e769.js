/* @oracle/oraclejet-preact: undefined */
import { jsx } from 'preact/jsx-runtime';
import { E as EmbeddedIconButton } from './EmbeddedIconButton-46434710.js';
import { S as SvgChevronDown } from './ChevronDown-16a46328.js';
import { S as SvgChevronRight } from './ChevronRight-f4ff20cb.js';
import { c as classNames } from './classNames-4e12b00d.js';

const ITEM_SELECTOR = 'oj-collection-expander';
/**
 * The Expander component renders expand and collapse icons in collections.
 */
function Expander({ itemKey, expanded, onToggle }) {
    const classes = classNames([ITEM_SELECTOR]);
    const actionHandler = () => {
        onToggle({ value: itemKey });
    };
    return (jsx("span", { class: classes, children: jsx(EmbeddedIconButton, { "aria-label": expanded ? 'collapsible' : 'expandable', onAction: actionHandler, children: expanded ? jsx(SvgChevronDown, { size: "6x" }) : jsx(SvgChevronRight, { size: "6x" }) }) }));
}
const isExpander = (elem) => {
    if (elem.parentElement && elem.parentElement.classList.contains(ITEM_SELECTOR)) {
        return true;
    }
    return false;
};

export { Expander as E, ITEM_SELECTOR as I, isExpander as i };
//# sourceMappingURL=Expander-1347e769.js.map
