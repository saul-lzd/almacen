/* @oracle/oraclejet-preact: undefined */
'use strict';

/**
 * @license
 * Copyright (c) %FIRST_YEAR% %CURRENT_YEAR%, Oracle and/or its affiliates.
 * Licensed under The Universal Permissive License (UPL), Version 1.0
 * as shown at https://oss.oracle.com/licenses/upl/
 * @ignore
 */
const inSpace = (x, y, dimensions) => {
    const minX = dimensions.x;
    const minY = dimensions.y;
    const maxX = dimensions.x + dimensions.width;
    const maxY = dimensions.y + dimensions.height;
    if (x < minX || x > maxX || y < minY || y > maxY) {
        return false;
    }
    return true;
};
/**
 * Returns the shape ramp used for markers for line/area charts.
 * @returns {readonly string[]} The shape ramp.
 */
function getShapeRamp() {
    return ['square', 'circle', 'diamond', 'plus', 'triangleDown', 'triangleUp'];
}

exports.getShapeRamp = getShapeRamp;
exports.inSpace = inSpace;
//# sourceMappingURL=utils-da07b35e.js.map
