/* @oracle/oraclejet-preact: undefined */
'use strict';

var mergeProps = require('./mergeProps-e3da7237.js');
var useChartDataCursor = require('./useChartDataCursor-67f7cc75.js');
var useChartDatatip = require('./useChartDatatip-1bceba5d.js');
var eventsUtils = require('./eventsUtils-b87ea91f.js');
require('./accUtils-4a359187.js');
var useChartDrill = require('./useChartDrill-c052942f.js');
var useSelection = require('./useSelection-da64e28e.js');

/**
 * @license
 * Copyright (c) %FIRST_YEAR% %CURRENT_YEAR%, Oracle and/or its affiliates.
 * Licensed under The Universal Permissive License (UPL), Version 1.0
 * as shown at https://oss.oracle.com/licenses/upl/
 * @ignore
 */
function useChartDatatipAndCursor({ series, leafGroupsInfo, width, height, findNearest, getMarkerInfo, onDataCursorPositionChange, isDataCursorEnabled, dataCursorPosition, dataCursorStyle, hoveredItemInfo, focusedItemInfo, getDataItem, getRefItem, getDataItemPos, getRefObjectBounds, getRefAxis, datatip, valueFormats, isHoriz, xScale, yScale, y2Scale, plotAreaSpace, timeAxisType, touchResponse, isDisabled, defaultXFormatter, defaultYFormatter, defaultY2Formatter, hasXYValues, getGroupFromPath, translations }) {
    const { datatipContent, datatipProps } = useChartDatatip.useChartDatatip({
        series,
        leafGroupsInfo,
        width,
        height,
        isDataCursorEnabled,
        dataCursorPosition,
        hoveredItemInfo,
        getDataItem,
        getRefItem,
        getDataItemPos,
        getRefObjectBounds,
        getRefAxis,
        focusedItemInfo,
        datatip,
        valueFormats,
        isHoriz,
        xScale,
        findNearest,
        timeAxisType,
        touchResponse,
        isDisabled,
        defaultXFormatter,
        defaultYFormatter,
        defaultY2Formatter,
        hasXYValues,
        translations,
        getGroupFromPath
    });
    const { dataCursorContent, dataCursorProps } = useChartDataCursor.useChartDataCursor({
        touchResponse,
        findNearest,
        dataCursorPosition,
        onDataCursorPositionChange,
        isDataCursorEnabled,
        dataCursorStyle,
        isHoriz,
        getMarkerInfo,
        focusedItemInfo,
        plotAreaSpace,
        yScale,
        y2Scale,
        xScale,
        getDataItemPos
    });
    return {
        datatipContent,
        dataCursorContent,
        datatipProps: mergeProps.mergeProps(dataCursorProps, datatipProps)
    };
}

/**
 * @license
 * Copyright (c) %FIRST_YEAR% %CURRENT_YEAR%, Oracle and/or its affiliates.
 * Licensed under The Universal Permissive License (UPL), Version 1.0
 * as shown at https://oss.oracle.com/licenses/upl/
 * @ignore
 */
function useSelectDrill({ selectionMode, selection, onChange, rootRef, drilling, onItemDrill, onGroupDrill, focusedGroupInfo, focusedItemInfo, levelsArray, getDataItem, getDataById, ignoreAsTrigger }) {
    const idExtracter = (event) => {
        const isItemFocused = focusedItemInfo.isCurrent;
        const info = isItemFocused && event.type === 'keyup'
            ? focusedItemInfo
            : eventsUtils.getInfo(rootRef, event.target);
        return info && info.seriesIndex != null && info.groupIndex != null
            ? getDataItem(info.seriesIndex, info.groupIndex, levelsArray[levelsArray.length - 1][info.groupIndex].path)?.id
            : undefined;
    };
    const { selectionProps, selectionContent } = useSelection.useSelection({
        idExtracter,
        isDrillEnabled: drilling === 'on',
        selection,
        selectionMode,
        onChange,
        getDataById,
        ignoreAsTrigger
    });
    const drillProps = useChartDrill.useChartDrill({
        rootRef,
        selectionMode,
        drilling,
        focusedItemInfo,
        focusedGroupInfo,
        levelsArray,
        getDataItem,
        onItemDrill,
        onGroupDrill
    });
    return { selectDrillProps: mergeProps.mergeProps(selectionProps, drillProps), selectionContent };
}

exports.useChartDatatipAndCursor = useChartDatatipAndCursor;
exports.useSelectDrill = useSelectDrill;
//# sourceMappingURL=useSelectDrill-0b4b47ec.js.map
