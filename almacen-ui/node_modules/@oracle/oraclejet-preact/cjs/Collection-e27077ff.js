/* @oracle/oraclejet-preact: undefined */
'use strict';

var jsxRuntime = require('preact/jsx-runtime');
var preact = require('preact');

/**
 * Component that renders an array of data.
 */
function Collection({ items, indexOffset = 0, children }) {
    return (jsxRuntime.jsx(preact.Fragment, { children: items.map((data, index) => {
            const ctx = { index: index + indexOffset, data };
            return children(ctx);
        }) }));
}

exports.Collection = Collection;
//# sourceMappingURL=Collection-e27077ff.js.map
