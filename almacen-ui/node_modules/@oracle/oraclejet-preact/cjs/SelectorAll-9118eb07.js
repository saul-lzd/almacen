/* @oracle/oraclejet-preact: undefined */
'use strict';

var jsxRuntime = require('preact/jsx-runtime');
var hooks = require('preact/hooks');
var useTooltip = require('./useTooltip-34accecc.js');
require('./logger-2b636482.js');
require('./LayerHost-3cd73603.js');
require('preact/compat');


require('./hooks/UNSAFE_useTooltip/themes/redwood/TooltipContentVariants.css.js');
require('./tooltipUtils-2bc15136.js');
var useTranslationBundle = require('./useTranslationBundle-12dd087d.js');
var BaseSelector = require('./BaseSelector-2dc69917.js');

/**
 * The SelectorAll component renders checkboxes for select all case.
 */
function SelectorAll({ 'aria-label': ariaLabel, 'aria-labelledby': ariaLabelledby, showTooltip = 'disabled', testId, onChange, selected }) {
    const selectionHandler = hooks.useCallback((event) => {
        const { key } = event;
        if (key !== undefined && key !== ' ') {
            return;
        }
        if (selected == 'none' || selected == 'partial') {
            onChange?.({ value: { all: true, deletedKeys: new Set() }, target: event.target });
        }
        else {
            onChange?.({ value: { all: false, keys: new Set() }, target: event.target });
        }
        //selectionHandler would be triggered by the onClick
        event.stopPropagation();
    }, [onChange, selected]);
    const isChecked = selected === 'partial' || selected === 'partial-all'
        ? 'partial'
        : selected === 'all'
            ? 'checked'
            : 'unchecked';
    const translations = useTranslationBundle.useTranslationBundle('@oracle/oraclejet-preact');
    const tooltipLabel = selected === 'none' || selected === 'partial'
        ? translations.selectorAll_selectAll()
        : translations.selectorAll_deselectAll();
    const { tooltipContent, tooltipProps } = useTooltip.useTooltip({
        text: tooltipLabel,
        isDisabled: showTooltip === 'disabled' ? true : false
    });
    const ariaLabelText = !ariaLabel && !ariaLabelledby ? tooltipLabel : undefined;
    return (jsxRuntime.jsx(BaseSelector.BaseSelector, { "aria-label": ariaLabel ? ariaLabel : ariaLabelText, "aria-labelledby": ariaLabelledby, onSelectionHandler: selectionHandler, isChecked: isChecked, tooltipContent: tooltipContent, tooltipProps: tooltipProps, testId: testId }));
}

exports.SelectorAll = SelectorAll;
//# sourceMappingURL=SelectorAll-9118eb07.js.map
